# Copy this file to terraform.tfvars and update with your values
# AWS Configuration
aws_region  = "us-east-2"
aws_profile = "vetrii"

# Project Configuration
project_name = "vetrii-backend"

# Environment Configuration
environment = "staging"

# S3 Bucket Configuration
bucket_name = "vetrii-backend-staging-bucket"

# Tags
tags_extra = {
  Project     = "Vetrii Backend"
  ManagedBy   = "Terraform"
  Owner       = "Vetrii"
  Environment = "staging"
}
